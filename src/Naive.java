import java.util.ArrayList;

public class Naive extends Algorithm {
    public Naive(CollectionToCover toCover, ArrayList<CollectionSet> collectionFamily) {
        super(toCover, collectionFamily);
    }

    @Override
    public int[] cover() {
        for (int i = 0; i < collectionFamily.size(); ++i) {

        }
    }


}
